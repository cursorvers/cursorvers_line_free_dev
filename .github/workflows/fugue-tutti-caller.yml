name: Fugue Tutti Caller

on:
  issues:
    types: [labeled]

permissions:
  issues: write
  contents: read

jobs:
  tutti:
    if: >-
      github.actor != 'github-actions[bot]' &&
      contains(github.event.issue.labels.*.name, 'fugue-task') &&
      contains(github.event.issue.labels.*.name, 'tutti')
    uses: cursorvers/fugue-orchestrator/.github/workflows/fugue-tutti-router.yml@7cf653f09aad9af23102bf672b1a893abd0ec511
    with:
      issue_number: "${{ format('{0}', github.event.issue.number) }}"
    secrets: inherit
